---
name: ai-pm-case-study
description: AI PM(프로덕트 매니저) 또는 노코드+AI API 기반 수익화 프로젝트를 케이스 스터디, 포트폴리오 문서, 프로젝트 회고 문서로 작성할 때 사용합니다. 사용자가 프로젝트 아이디어나 진행 상황을 던지면, 기획 검증→UX 설계→QA 검증→마케팅 런칭→성장 증명→데모데이 크리틱의 6단계 방법론 기준으로 빠진 정보를 분석하고, 무엇을 더 알려줘야 하는지 질문한 뒤, 답변을 반영해 완성된 문서를 작성합니다. "케이스 스터디", "PM 포트폴리오", "프로젝트 회고", "수익화 프로젝트 정리", "AI PM 프로젝트 문서화", "프로젝트를 문서로 정리해줘" 같은 표현이 나오면 다른 방법보다 이 스킬을 우선 사용하세요.
---

# AI PM 케이스 스터디 작성

노코드/AI API 기반 수익화 프로젝트를 6단계 방법론에 따라 구조화된 케이스 스터디 문서로 만든다. 핵심 흐름은 "아이디어 접수 → 갭 분석 → 부족한 부분 질문 → 입력 반영 → 문서 작성"이다.

**절대 한 번에 다 물어보고 한 번에 다 쓰지 않는다.** 사용자가 준 정보로 먼저 분석부터 하고, 무엇이 빠졌는지 판단한 다음에 질문한다. 질문 없이 바로 완성된 문서를 쓰지 않는다 (정보가 매우 충분한 경우는 예외).

## 워크플로우

### 1단계 — 아이디어 접수
사용자가 자유 형식으로 프로젝트 아이디어/진행 내용을 준다. 완성된 프로젝트든, 진행 중이든, 아직 구상 단계든 상관없이 받는다. 이것부터 자연스럽게 파악한다 (별도 질문으로 끊지 말고 다른 질문과 묶어서):
- 프로젝트가 **완료 / 진행 중 / 구상 단계** 중 어디에 해당하는지
- **출력 규격** — 용도(포트폴리오/데모데이 제출/내부 회고 중 무엇인지), 목표 분량(A4 기준 대략 몇 장), 어투(1인칭 회고체 vs 3인칭 보고서체). 사용자가 신경 안 쓰면 기본값으로 진행한다: 용도=포트폴리오, 분량=A4 3~5장, 어투=1인칭 회고체.
- **노코드 범위 체크** — 아이디어가 노코드+AI API로 구현 가능한 범위인지 가볍게 판단한다. 벗어나면(예: 상시 실행 데스크톱 오버레이, 네이티브 앱) 문서 작성은 그대로 진행하되, 1단계 피드백에서 구현 난이도 리스크를 조기에 짚어준다.

### 2단계 — 갭 분석 (내부 판단, 사용자에게 보여주지 않음)
받은 내용을 `references/checklist.md`의 산출물 목록과 아래 6단계 기준으로 대조한다. 이미 채워진 항목과 비어있는 항목을 구분한다.

### 3단계 — 부족한 부분 안내
비어있는 항목을 **단계 순서대로, 단계별로 묶어서** 질문한다.
- 6단계를 한꺼번에 다 묻지 않는다. 특히 1단계(기획 검증) 정보가 부족하면 그것부터 먼저 묻는다.
- 프로젝트가 아직 도달하지 않은 단계(예: 아이디어 단계인데 성장 증명 관련 정보)는 "아직 안 했으면 계획만 알려줘도 됨"이라고 안내한다.
- 질문은 구체적으로 쓴다 — "마케팅은 어떻게 했나요?"보다 "초기 유저를 어디서 어떻게 모았고, 반응은 어땠나요?"처럼.
- 세부 질문 문구는 `references/stage-templates.md`의 단계별 질문 목록을 참고한다.

### 4단계 — 입력 반영
사용자 답변을 기다린다. 답변이 부분적이어도 된다. 사용자가 "이 정도면 됐어, 나머지는 알아서 채워줘"라고 하면:
- 실제 수치·성과·고객 반응은 지어내지 않는다.
- 합리적으로 유추 가능한 항목(예: 일반적인 KPI 후보, 표준 QA 시나리오 구조)만 채우고 문서에 **[가정]** 표시를 남긴다.
- 그래도 못 채우는 항목은 **[추가 입력 필요]**로 남겨둔다.

### 5단계 — 문서 작성
**이 단계의 산출물은 대화에 보여주는 마크다운 초안이다** (파일 생성은 6단계에서). 문서 **맨 앞에 원페이저 요약**(문제·해결·임팩트·내 역할)을 먼저 넣고, 그 뒤에 `references/stage-templates.md`의 6단계 템플릿을 따라 작성한다. `[가정]`·`[추가 입력 필요]` 태그는 이 초안에 그대로 노출해 사용자가 검토할 수 있게 한다. 프로젝트가 아직 도달하지 않은 단계는 "결과" 대신 "계획"으로 서술한다. 작성할 때 톤·분량·디테일 수준의 기준은 `references/example-case-study.md`를 참고한다 (그대로 복붙하지 말고 구체성의 감만 잡는다).

### 6단계 — 완성도 체크 & 출력
**5단계 초안이 확정된 내용을 `references/html-template.html`에 부어 파일화하는 단계다** — 내용을 새로 쓰지 않는다. 먼저 `references/checklist.md`의 항목이 문서에 반영됐는지 확인한다. 빠진 게 있으면 사용자에게 짚어준다.

**태그 정리 (중요)**: `[가정]`·`[추가 입력 필요]` 표기는 초안 검토용이다. 최종 파일(HTML/PDF)을 만들기 전에 사용자에게 남은 `[추가 입력 필요]` 항목을 한 번 더 확인받고, 채워진 `[가정]` 항목은 "이대로 확정할지" 물어본 뒤 **최종본에서는 이 태그들을 모두 제거**한다. 포트폴리오·데모데이 제출본에 이런 태그가 남으면 안 된다. (사용자가 "초안이니 태그 남겨둬"라고 하면 예외.)

출력은 기본적으로 **HTML + PDF** 두 가지로 만든다. 구체적인 방법은 `references/output-guide.md`를 따른다. 요점만:
- HTML: `references/html-template.html`을 복사해 `{{...}}`를 채운다 (한글 폰트 스택을 지우지 말 것).
- PDF: Windows는 Chrome→Edge 헤드리스로 자동 변환, macOS는 Chrome 자동 변환 → 없으면 Safari로 열어 `Cmd+P` 수동 저장 안내.
- 저장 파일명: `<프로젝트명>_case-study.html` / `.pdf`. 두 파일 모두 사용자에게 전달한다.

## 6단계 프레임워크 (요약)

| 단계 | 핵심 산출물 |
|---|---|
| 1. 기획 검증 | PRD, 페르소나, KPI 가설, 로드맵 |
| 2. UX 설계 | User flow, 트레킹 설계, 과금/예외 로직 |
| 3. QA 검증 | QA 시나리오, 프로토타입 검증, 상품화 승인 |
| 4. 마케팅 런칭 | PoC 마케팅 플랜, 초기 유저 반응 |
| 5. 성장 증명 | 퀵윈 개선, ROI, BM 구체화 |
| 6. 데모데이 크리틱 | 최종 성과, 론칭 URL, 회고 |

자세한 질문 목록과 섹션별 작성 템플릿은 `references/stage-templates.md`를 읽는다. 최종 산출물 체크리스트는 `references/checklist.md`를 읽는다.

## 실무 품질 원칙

단순히 6단계를 채우는 걸 넘어 "합격하는" 케이스 스터디가 되려면 **의사결정 서술 · 원페이저 요약 · 정량 데이터 · 본인 기여 분리 · 용도별 강조**를 지켜야 한다. 작성 전 `references/quality-principles.md`를 읽고 그 기준을 따른다.

## 주의사항
- 사용자가 제공하지 않은 숫자·성과·고객 반응을 절대 지어내지 않는다.
- 아직 결과가 없는 초기 단계 프로젝트도 지원한다 — 이 경우 "결과" 섹션을 "목표/계획"으로 바꿔 쓴다.
- 질문할 때 한 번에 너무 많이 묻지 않는다 (단계별로 나눠서 순차적으로).

## 유지보수 규칙 (이 스킬을 수정할 때)

- **6단계 섹션 구조의 단일 원본은 `references/stage-templates.md`다.** 구조를 바꿀 때(섹션 추가·삭제·이름 변경)는 stage-templates.md를 먼저 고치고, 아래 파일들을 그에 맞춰 함께 갱신한다 — 하나라도 빠지면 질문과 문서가 어긋난다:
  - `SKILL.md`의 "6단계 프레임워크 (요약)" 표
  - `references/html-template.html`의 섹션 마크업
  - `references/example-case-study.md`의 예시 섹션
  - `references/checklist.md`의 점검 항목
- **reference 파일을 추가·삭제·이름 변경할 때**는 다음도 함께 갱신한다: 저장소 README의 구조 트리, `integrations/`의 Codex·Gemini 포인터 "읽을 파일" 목록, SKILL.md 안의 해당 파일 참조.
- 스킬을 수정했으면 배포 저장소에도 반영한다 (저장소 README의 "수정·재배포" 절차 참고).
